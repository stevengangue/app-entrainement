name: app-entrainement
php: '8.4'

database:
  mysql: true

build:
  - composer install --no-interaction --optimize-autoloader

hooks:
  deploy: |
    php artisan migrate --force
    php artisan optimize